import os

def make_local_path(s):
    """Converts '/' in a string to os.sep"""
    return s.replace('/', os.sep)


def make_local_path(s):
    """Converts '/' in a string to os.sep"""
    return s.replace('/', os.sep)

