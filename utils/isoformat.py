from typing import Union

def isoformat(o: Union[datetime.date, datetime.time]) -> str:
    return o.isoformat()


def isoformat(o: Union[datetime.date, datetime.time]) -> str:
    return o.isoformat()

