
def _deterministic_hash(s: str) -> int:
    return int(hashlib.md5(s.encode(), usedforsecurity=False).hexdigest(), 16)


def _deterministic_hash(s: str) -> int:
    return int(hashlib.md5(s.encode(), usedforsecurity=False).hexdigest(), 16)


def _deterministic_hash(s: str) -> int:
    return int(hashlib.md5(s.encode(), usedforsecurity=False).hexdigest(), 16)

