
def flush_logs() -> None:
    try:
        _LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
    except Exception:
        pass
    flush_reasoning_logs(_log_buffer, _LOG_PATH, logger)


def flush_logs() -> None:
    try:
        _LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
    except Exception:
        pass
    flush_reasoning_logs(_log_buffer, _LOG_PATH, logger)


def flush_logs() -> None:
    try:
        _LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
    except Exception:
        pass
    flush_reasoning_logs(_log_buffer, _LOG_PATH, logger)


def flush_logs() -> None:
    try:
        _LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
    except Exception:
        pass
    flush_reasoning_logs(_log_buffer, _LOG_PATH, logger)


def flush_logs() -> None:
    try:
        _LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
    except Exception:
        pass
    flush_reasoning_logs(_log_buffer, _LOG_PATH, logger)


def flush_logs() -> None:
    try:
        _LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
    except Exception:
        pass
    flush_reasoning_logs(_log_buffer, _LOG_PATH, logger)

