
def _score_evolve(v: float, action: str) -> float:
    if action.startswith("evolve:"):
        v += 0.6
    return v


def _score_evolve(v: float, action: str) -> float:
    if action.startswith("evolve:"):
        v += 0.6
    return v

