
def _detect_draw(belief_tracker, hand_size_diff: int):
    if hand_size_diff > 0:
        belief_tracker.update_on_draw(hand_size_diff)


def _detect_draw(belief_tracker, hand_size_diff: int):
    if hand_size_diff > 0:
        belief_tracker.update_on_draw(hand_size_diff)

